// This component is now intentionally minimal - stats are in the hero
// Keeping file to avoid import errors, but not displaying redundant metrics

export default function ImpactMetrics() {
  return null
}
