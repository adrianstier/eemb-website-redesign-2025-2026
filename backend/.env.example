# =============================================================================
# EEMB Strapi Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
HOST=0.0.0.0
PORT=1337

# -----------------------------------------------------------------------------
# SECURITY SECRETS - CRITICAL
# -----------------------------------------------------------------------------
# Generate each of these with: openssl rand -base64 32
# NEVER use the placeholder values in production!
#
# APP_KEYS: Comma-separated list of 4 unique keys for encryption
# Example generation: for i in {1..4}; do openssl rand -base64 32; done
APP_KEYS=GENERATE_WITH_openssl_rand_-base64_32,GENERATE_SECOND_KEY,GENERATE_THIRD_KEY,GENERATE_FOURTH_KEY

# API_TOKEN_SALT: Salt for hashing API tokens
API_TOKEN_SALT=GENERATE_WITH_openssl_rand_-base64_32

# ADMIN_JWT_SECRET: Secret for signing admin panel JWTs
ADMIN_JWT_SECRET=GENERATE_WITH_openssl_rand_-base64_32

# TRANSFER_TOKEN_SALT: Salt for data transfer tokens
TRANSFER_TOKEN_SALT=GENERATE_WITH_openssl_rand_-base64_32

# JWT_SECRET: Secret for signing user JWTs
JWT_SECRET=GENERATE_WITH_openssl_rand_-base64_32

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=eemb_strapi
# SECURITY: Use strong, unique credentials in production!
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DATABASE_SSL=false
# For production with SSL:
# DATABASE_SSL=true
# DATABASE_SSL_REJECT_UNAUTHORIZED=true

# -----------------------------------------------------------------------------
# Cloudinary (Media Storage)
# -----------------------------------------------------------------------------
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_NAME=your_cloud_name
CLOUDINARY_KEY=your_api_key
CLOUDINARY_SECRET=your_api_secret

# -----------------------------------------------------------------------------
# SendGrid (Email)
# -----------------------------------------------------------------------------
# Get API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_DEFAULT_FROM=noreply@eemb.ucsb.edu
SENDGRID_DEFAULT_REPLY_TO=eemb@ucsb.edu

# -----------------------------------------------------------------------------
# Sentry (Error Tracking)
# -----------------------------------------------------------------------------
# Get DSN from: https://sentry.io/settings/projects/
SENTRY_DSN=your_sentry_dsn

# -----------------------------------------------------------------------------
# Admin Panel
# -----------------------------------------------------------------------------
ADMIN_URL=/admin

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
NODE_ENV=development

# =============================================================================
# PRODUCTION SECURITY CHECKLIST
# =============================================================================
# Before deploying to production:
# [ ] All secrets generated with openssl rand -base64 32
# [ ] Database password is strong and unique
# [ ] DATABASE_SSL=true for remote databases
# [ ] NODE_ENV=production
# [ ] Sentry DSN configured for error tracking
# [ ] CORS configured to only allow production domains
# [ ] Rate limiting enabled
# =============================================================================